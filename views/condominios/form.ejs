<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title><%= acao === 'novo' ? 'Novo Condomínio' : 'Editar Condomínio' %> - Controle de Presença</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link 
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" 
    rel="stylesheet"
  >
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>

  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/dashboard">Controle de Presença</a>
      <div class="d-flex">
        <span class="navbar-text text-white me-3">
          Olá, <%= usuario.nome %> (<%= usuario.perfil %>)
        </span>
        <a href="/auth/logout" class="btn btn-outline-light btn-sm">Sair</a>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <h1 class="h4 mb-4">
      <%= acao === 'novo' ? 'Novo Condomínio' : 'Editar Condomínio' %>
    </h1>

    <form 
      action="<%= acao === 'novo' ? '/condominios/novo' : '/condominios/editar/' + condominio.id %>" 
      method="POST"
      class="row g-3"
    >
      <div class="col-md-6">
        <label for="nome" class="form-label">Nome do Condomínio *</label>
        <input 
          type="text" 
          class="form-control" 
          id="nome" 
          name="nome" 
          required
          value="<%= condominio ? condominio.nome : '' %>"
        >
      </div>

      <div class="col-md-3">
        <label for="codigo_interno" class="form-label">Código Interno</label>
        <input 
          type="text" 
          class="form-control" 
          id="codigo_interno" 
          name="codigo_interno"
          value="<%= condominio ? (condominio.codigo_interno || '') : '' %>"
        >
      </div>

      <div class="col-md-3">
        <label for="estado" class="form-label">Estado (UF)</label>
        <input 
          type="text" 
          class="form-control" 
          id="estado" 
          name="estado"
          maxlength="2"
          value="<%= condominio ? (condominio.estado || '') : '' %>"
        >
      </div>

      <div class="col-md-6">
        <label for="cidade" class="form-label">Cidade</label>
        <input 
          type="text" 
          class="form-control" 
          id="cidade" 
          name="cidade"
          value="<%= condominio ? (condominio.cidade || '') : '' %>"
        >
      </div>

      <div class="col-md-12">
        <label for="endereco" class="form-label">Endereço</label>
        <textarea 
          class="form-control" 
          id="endereco" 
          name="endereco"
          rows="2"
        ><%= condominio ? (condominio.endereco || '') : '' %></textarea>
      </div>

      <% if (acao === 'editar') { %>
        <div class="col-md-2 form-check mt-2">
          <input 
            class="form-check-input" 
            type="checkbox" 
            id="ativo" 
            name="ativo"
            <%= condominio && condominio.ativo ? 'checked' : '' %>
          >
          <label class="form-check-label" for="ativo">
            Ativo
          </label>
        </div>
      <% } %>

      <div class="col-12 mt-3">
        <a href="/condominios" class="btn btn-secondary">Voltar</a>
        <button type="submit" class="btn btn-primary">
          Salvar
        </button>
      </div>
    </form>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
